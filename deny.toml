# cargo-deny configuration for Rust-LLM-Api-Router
# Install: cargo install cargo-deny
# Run: cargo deny check

[advisories]
ignore = [
    "RUSTSEC-2026-0066",  # astral-tokio-tar - transitive dep, low severity
    "RUSTSEC-2026-0049",  # rustls-webpki - transitive dep, limited impact
    "RUSTSEC-2024-0388",  # derivative - unmaintained, transitive dep
    "RUSTSEC-2024-0384",  # instant - unmaintained, transitive dep
]

[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    "MPL-2.0",
    "CDLA-Permissive-2.0",
]
confidence-threshold = 0.8

[bans]
multiple-versions = "warn"
wildcards = "allow"
highlight = "all"
workspace-default-features = "allow"
external-default-features = "allow"
deny = []
skip = []
skip-tree = []

[sources]
unknown-registry = "warn"
unknown-git = "warn"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
